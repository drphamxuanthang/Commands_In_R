---
title: "Importing data"
output: html_notebook
---

This notebook includes command related to importing data

# Identify the path to file
```{r}
path_rawfile = file.choose()
```

# Open the .CSV file and save to an object
```{r}
rawfile <- read.csv(path_rawfile)
str(rawfile)
head(rawfile)
```

